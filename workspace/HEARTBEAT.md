# 💓 HEARTBEAT — Проактивные проверки

*Чек-лист для регулярных проверок системы.*

---

## Автоматические проверки (каждые 30 мин)

### 🔴 Критические (алерт в Telegram)

```bash
# Docker контейнеры
docker ps --format "{{.Names}}" | grep -q qwen3_omni || ALERT "LLM down!"
docker ps --format "{{.Names}}" | grep -q milvus || ALERT "Milvus down!"

# SSD примонтирован
ls /media/agx-thor/SSD_AI/qwen30_nvfp4 || ALERT "SSD not mounted!"

# Диск не переполнен
df -h / | awk 'NR==2 {if ($5+0 > 90) print "Disk >90%!"}'
```

### 🟡 Важные (запись в лог)

```bash
# API отвечают
curl -s http://localhost:8010/v1/models | grep -q qwen
curl -s http://localhost:8015/health | grep -q version
curl -s http://localhost:19530/v1/vector/collections

# Память GPU
nvidia-smi --query-gpu=memory.used,memory.total --format=csv
```

### 🟢 Информационные

```bash
# Uptime
uptime

# Сколько запросов сегодня
grep "$(date +%Y-%m-%d)" ~/animara/logs/rag_v9.log | wc -l
```

---

## Расписание проверок

| Время | Действие |
|-------|----------|
| */30 * * * * | Критические проверки |
| 0 9 * * * | Утренний брифинг |
| 0 21 * * * | Вечерний отчёт |
| 0 3 * * * | Ночное обслуживание |

---

## Утренний брифинг (09:00)

```
🌅 Доброе утро, Сергей!

📊 Статус системы:
- LLM: ✅ работает (45 tok/s)
- Milvus: ✅ 1,234 записей
- Диск: 67% использовано

📋 Задачи на сегодня:
1. [!] Позвонить в банк (дедлайн сегодня!)
2. Настроить DeepStream визуализацию
3. Написать документацию

🗓️ События:
- 14:00 — созвон с командой

Хорошего дня! 🤖
```

---

## Вечерний отчёт (21:00)

```
🌙 Вечерний отчёт

📈 Сегодня:
- 47 сообщений обработано
- 12 фактов сохранено в память
- 3 задачи выполнено

✅ Выполнено:
- Изучена архитектура Moltbot
- Создана новая структура workspace
- Сохранено на GitHub

⏳ Перенесено на завтра:
- Настройка DeepStream

💤 Система работает стабильно. Спокойной ночи!
```

---

## Ночное обслуживание (03:00)

```bash
# Очистка старых логов (>7 дней)
find ~/animara/logs -name "*.log" -mtime +7 -delete

# Компактация Milvus (если нужно)
# curl -X POST http://localhost:19530/v1/vector/collections/conversations/compact

# Бэкап workspace
tar -czf ~/animara/backups/workspace_$(date +%Y%m%d).tar.gz ~/animara/workspace/

# Очистка старых бэкапов (>30 дней)
find ~/animara/backups -name "*.tar.gz" -mtime +30 -delete
```

---

## Ручные проверки

### После перезагрузки

```bash
# Полный чек-лист
echo "1. SSD:"
ls /media/agx-thor/SSD_AI/qwen30_nvfp4 && echo "✅" || echo "❌ MOUNT!"

echo "2. LLM:"
curl -s http://localhost:8010/v1/models | grep -q qwen && echo "✅" || echo "❌"

echo "3. Milvus:"
curl -s http://localhost:19530/v1/vector/collections | grep -q memories && echo "✅" || echo "❌"

echo "4. RAG Proxy:"
curl -s http://localhost:8015/health | grep -q version && echo "✅" || echo "❌"

echo "5. Memory test:"
curl -s http://localhost:8015/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"qwen3","person_id":"owner_sergey","messages":[{"role":"user","content":"Как меня зовут?"}]}' \
  | grep -qi сергей && echo "✅" || echo "❌"
```

### При проблемах

```bash
# Логи LLM
docker logs qwen3_omni --tail 50

# Логи RAG Proxy
tail -50 ~/animara/logs/rag_v9.log

# Логи Telegram
tail -50 ~/animara/logs/telegram_bot.log

# GPU память
nvidia-smi

# Процессы Python
ps aux | grep python
```

---

## Алерты

### Формат Telegram алерта

```
🚨 [КРИТИЧНО] <описание>
Время: HH:MM
Компонент: <имя>
Действие: <что сделать>
```

### Примеры

```
🚨 [КРИТИЧНО] LLM контейнер упал!
Время: 14:35
Компонент: qwen3_omni
Действие: docker start qwen3_omni

⚠️ [ВАЖНО] Диск заполнен на 85%
Время: 09:00
Компонент: /dev/sda1
Действие: Очистить логи или увеличить место

ℹ️ [ИНФО] Бэкап workspace создан
Время: 03:00
Файл: workspace_20260131.tar.gz
```

---

*Этот файл — инструкция для проактивного поведения.*
