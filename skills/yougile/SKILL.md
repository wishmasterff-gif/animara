---
name: yougile
description: Управление задачами YouGile
version: 1.0.0
metadata:
  animara:
    requires:
      env: ["YOUGILE_API_KEY"]
---

# 📋 YouGile

Управление задачами в YouGile.

## Команды

- `yougile_tasks()` — список задач
- `yougile_create(title, column_id)` — создать задачу
- `yougile_find(search)` — найти задачу
