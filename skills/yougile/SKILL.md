# 📋 YouGile Skill

## Описание
Управление задачами в YouGile — таск-трекере для команд.

## Функции

### Получение данных

#### `get_tasks(limit: int = 25) -> str`
Получить список всех задач.

#### `get_columns() -> str`
Получить список всех колонок (статусов).

#### `find_task(search_term: str) -> dict`
Найти задачу по части названия.

### Создание и изменение

#### `create_task(title: str, column_id: str, description: str = "") -> str`
Создать новую задачу.

#### `move_task(task_id: str, column_id: str) -> str`
Переместить задачу в другую колонку.

#### `append_to_description(task_id: str, text: str) -> str`
Добавить текст в описание задачи.

#### `set_deadline(task_id: str, deadline: str) -> str`
Установить дедлайн (формат: YYYY-MM-DD).

#### `complete_task(task_id: str) -> str`
Отметить задачу выполненной.

## Пример использования

```python
from skills.yougile.scripts.main import (
    get_tasks, find_task, create_task, 
    move_task, append_to_description
)

# Список задач
tasks = get_tasks()
print(tasks)

# Найти задачу
task = find_task("квадроцикл")
print(task)  # {"id": "xxx", "title": "...", "description": "..."}

# Создать задачу
result = create_task(
    title="Купить молоко",
    column_id="abc-123",
    description="Не забыть про сыр"
)

# Добавить информацию в описание
append_to_description(
    task_id="xxx-yyy",
    text="🔧 Найденные варианты:\n1. Вариант А\n2. Вариант Б"
)
```

## Конфигурация
API токен хранится в `~/animara/config/secrets.json`:
```json
{
  "yougile_token": "eAbKs-KzViRbIzz+..."
}
```

## Ограничения API
- Нет API для комментариев в задачах
- Подзадачи создаются как обычные задачи
- Используем добавление в description как workaround

## Типичные сценарии

### Исследование + запись результата
1. `find_task("тема")` → получить ID задачи
2. Выполнить web_search для исследования
3. `append_to_description(task_id, результаты)` → сохранить

### Утренний брифинг
1. `get_tasks()` → все задачи
2. Отфильтровать по дедлайну = сегодня
3. Отправить в Telegram
